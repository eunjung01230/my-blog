# ALL Posts "학습순"에서 카테고리를 보여줄 순서. (사이드바 순서는 _data/categories.yml 이 따로 관리한다)
# 값은 _data/categories.yml 의 slug 를 그대로 쓴다.
# 같은 카테고리 안에서는 글 front matter 의 learningOrder(작은 숫자 먼저) → 번호 없는 글은 오래된 글부터.
# 여기 없는 카테고리는 목록 뒤에 _data/categories.yml 순서로 붙는다. 새 카테고리는 원하는 자리에 한 줄 넣으면 된다.
- dev-setup
- terminal
- git
- frontend
- design
- backend
- database
- deployment
- ai-tools
- project
- my-blog
